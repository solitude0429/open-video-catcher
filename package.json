{
  "name": "open-video-catcher",
  "version": "0.2.3",
  "private": true,
  "description": "CocoCut-like WebExtension for detecting and downloading accessible page media.",
  "type": "commonjs",
  "scripts": {
    "validate:manifest": "node scripts/validate-manifest.js",
    "lint": "node --check background/service-worker.js && node --check content/content-script.js && node --check page/page-hook.js && node --check popup/popup.js && node --check src/media-utils.js && node --check scripts/generate-update-manifest.js && node --check scripts/validate-manifest.js && node --check scripts/package-extension.js",
    "lint:webext": "npx --yes web-ext@latest lint --source-dir dist/build/firefox --self-hosted --warnings-as-errors",
    "test": "node --test tests/*.test.js",
    "build": "npm run validate:manifest && npm run lint && npm test && node scripts/package-extension.js",
    "verify": "npm run build && npm run lint:webext && npm audit --audit-level=moderate"
  },
  "devDependencies": {}
}
